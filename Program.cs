using System;
using LAlib;
using Lneural;

namespace Program
{
    class Program
    {
        static void Main(string[] args)
        {
            // // tests on addition
            // Console.WriteLine("+--- Addition tests ---+");
            // Matrix test1, test2;
            // test1 = new Matrix(new float[,]{{1,2},{3,4},{5,6}});
            // test2 = new Matrix(new float[,]{{5,6,1},{7,8,2}});
            // // tests on inner product and scalar multiplication
            // Console.WriteLine("+--- Inner product and scalar multiplication tests ---+");
            // test1.mul(test2);
            // test1.PrintMatrix();
            // Matrix.transpose(test1).PrintMatrix();
            // // tests on static inversion (static -1 scalar multiplication)
            // Console.WriteLine("+--- Matrix inversion ---+");
            // Matrix.invert(test1).PrintMatrix();
            // Matrix.transpose(Matrix.invert(test1)).PrintMatrix();
            // // tests on static transposition
            // Console.WriteLine("+--- Matrix transposition ---+");
        }
    }
}
